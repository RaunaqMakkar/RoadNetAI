function RoleDefinitions() {
    return (
        <div className="role-panel">
            <h3>Role Definitions</h3>
            <p>Configure granular permissions for the 4 default system roles.</p>
            <button className="btn-configure">Configure Roles</button>
        </div>
    );
}

export default RoleDefinitions;
